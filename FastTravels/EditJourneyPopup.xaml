<?xml version="1.0" encoding="UTF-8" ?>
<pages:PopupPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    xmlns:animations="http://rotorgames.com"
    x:Class="FastTravel.EditJourneyPopup">
    <pages:PopupPage.Animation>
        <animations:ScaleAnimation 
            PositionIn="Center"
            PositionOut="Center"
            ScaleIn="1.2"
            ScaleOut="0.8"
            DurationIn="400"
            DurationOut="300"
            EasingIn="SinOut"
            EasingOut="SinIn"
            HasBackgroundAnimation="True"/>
    </pages:PopupPage.Animation>
    <Frame Padding="20,20,20,20"
            HorizontalOptions="FillAndExpand"
            VerticalOptions="Center">
        <StackLayout Padding="0,10,0,0" BackgroundColor="White">
            <StackLayout Orientation="Horizontal">
                <Button x:Name="Cancel" Text="Cancel" Clicked="Cancel_Clicked" HorizontalOptions="EndAndExpand" Margin="0,0,10,0"/>
                <Button x:Name="Save" Text="Save" Clicked="Save_Clicked" HorizontalOptions="End"/>
            </StackLayout>
        </StackLayout>
    </Frame>
</pages:PopupPage>
